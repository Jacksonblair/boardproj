#headermenu.ui.menu
	.item.menu-title-item
		span#pretitleitem 
			a(href="/") EziBoard
	#boardlistdropdown.ui.dropdown.item(onclick="")
		.text
			if currentboard
				= currentboard.name
			else
				| Go to...
		#boardlistmenu.menu
			.item wot
			.item wot
			include boardlist
	#rightmobilemenu.right.menu
		.ui.dropdown.item
			.text
				i.fas.fa-bars
			.menu
				if user
					a.item(href="/users/profile") Profile
					a.item(href="/users/logout") Logout
				else
					a.item(href="/users/login") Login
					a.item(href="/users/signup") Sign up
	#rightmenu.right.menu
		if user
			a.item(href="/users/profile")
				.text Profile
			a.item(href="/users/logout")
				.text Logout
		else
			a.item(href="/users/login")
				.text Login
			a.item(href="/users/signup")
				.text Sign up

